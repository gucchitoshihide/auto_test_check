%h1 Listing week_reports

%table
  %thead
    %tr
      %th content
      %th{:colspan => "3"}
      %th{:colspan => "3"}
  %tbody
    - WeekReport.latest.each do |report|
      %tr
        %td
          = report.content
        %td
          = link_to 'Show', week_report_path(id: report.id)
        %td
          = link_to 'Edit', edit_week_report_path(id: report.id)
%br
= link_to 'New Week report', new_week_report_path
