%h1 Listing skill_profiles
%table
  %thead
    %tr
      %th title
      %th content
      %th{:colspan => "3"}
  %tbody
    - SkillProfile.latest.each do |skill_profile|
      %tr
        %td= skill_profile.title
        %td= skill_profile.content
        %td= link_to 'Show', skill_profile_path(id: skill_profile.id)
        %td= link_to 'Edit', edit_skill_profile_path(skill_profile)
        %td= link_to 'Destroy', skill_profile_path(id: skill_profile.id), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Skill profile', new_skill_profile_path
= link_to 'Home', root_path
